# Escreva um programa que leia um valor em metros e o exiba convertido em centímetros e milímetros
    vm = float(input('Adicione um valor:'))
    cm = vm * 100
    ml = vm * 1000
    dc = vm / 10
    print(input(f'Esse valor convertido em centímetros fica: {cm} \n E convertido em milímetros fica: {ml} \n Por último o valor em decametro fica: {dc}dc '))
    
